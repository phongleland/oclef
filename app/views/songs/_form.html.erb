<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :composer_id %><br>
    <%= f.collection_select :composer_id, Composer.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.collection_select :book_id, Book.all, :id, :title %>
  </div>
  <div class="field">
    <%= f.label :start_page %><br>
    <%= f.number_field :start_page %>
  </div>
  <div class="field">
    <%= f.label :end_page %><br>
    <%= f.number_field :end_page %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
